integer = 20
IO.inspect(integer)

string = "str value"
IO.inspect(string)

float = 2.2
IO.inspect(float)

boolean = true
IO.inspect(boolean)

atom = :key1
IO.inspect(atom)

tuple = {:hashKey, "hashValue"}
IO.inspect(tuple)

list = [1,2,3]
IO.inspect(list)

map = %{0 => "hashvaluemap"}
IO.inspect(map)

